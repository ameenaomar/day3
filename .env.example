# Neon. DATABASE_URL is the pooled connection the app uses; DIRECT_DATABASE_URL
# is the direct one migrations run against.
DATABASE_URL="postgresql://user:password@ep-xxx-pooler.eu-central-1.aws.neon.tech/simply_styled?sslmode=require"
DIRECT_DATABASE_URL="postgresql://user:password@ep-xxx.eu-central-1.aws.neon.tech/simply_styled?sslmode=require"

# Where magic links point. No trailing slash.
APP_URL="http://localhost:3000"

# Signing key for session and magic-link tokens. Generate with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('base64url'))"
AUTH_SECRET=""

# Transactional email for magic links and order confirmations.
RESEND_API_KEY=""
EMAIL_FROM="Simply Styled <hello@example.com>"

# Payment gateway. "mock" runs the whole flow locally with no credentials.
PAYMENT_PROVIDER="mock"
